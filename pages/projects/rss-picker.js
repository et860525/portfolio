import { Container, Text, Heading } from '@chakra-ui/react'
import Layout from '../../components/layout'

const Project = () => (
  <Layout title="RSS Picker">
    <Container maxW="container.md" pt={20}>
      <Heading as="h2">RSS Picker</Heading>
    </Container>
  </Layout>
)

export default Project
